# This repository has moved

On 2026-09-11 `lolly-chrome-extension` was folded into the main Lolly repository and archived. Its content now lives at:

    https://github.com/lolly-tools/lolly/tree/main/shells/chrome-extension

The last commit imported from here was `728f16b36246167c1d9a9b358ffe2f6a29881d73`. Every commit of this repository's history was carried across with `git subtree add`, so `git log --all --full-history -- <file>` in the main repository finds it; a bundle of this repository as it stood on that day is kept by the maintainer.

This repository stays public and read-only so that existing clone URLs, issue links and blame permalinks keep resolving. Please open new issues and pull requests on https://github.com/lolly-tools/lolly.
